<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kodexlabs.internshipcamp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/img_icamp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity_Landing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Student_Login"/>
        <activity android:name=".Activity_Student_Reg"/>
        <activity android:name=".Activity_Startup"/>
    </application>

</manifest>